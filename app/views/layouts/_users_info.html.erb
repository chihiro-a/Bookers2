<!--部分テンプレート-->

<!--あとで部分テンプレートする↓ログインしている人専用-->

        <h2>User info</h2>
        <div>
        <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "NoImage.jpg" %>
        </div>
        <!--NoImageは.jpegだけど.jpg-と記述するっぽい…-->
        <table width=100%>
            <tr class="m-1">
                <th>name</th>
                <th><%= user.name %></th>
            </tr>
            <tr>
                <th>introduction</th>
                <th><%= user.introduction %></th>
            </tr>
        </table>
        
        <div class="row">
            <a class="btn btn-outline-secondary btn-block mt-5" href="/users/<%= current_user.id%>/edit "><span><i class="fas fa-user-cog"></i></span></a>
            <!--編集はログインしているユーザの編集ページのみアクセス-->
        </div>
        
        <!--新規投稿フォームテンプレート-->
        <h2 class="mt-3">New book</h2>
        <%= form_with model: book, local:true do |f| %>
        <!--@bookをbookに変更-->
        <p>Title</p>
        <div><%= f.text_field :title %></div>
        <p>Opinion</p>
        <div><%= f.text_area :body %></div>
        <div><%= f.submit 'Create Book',class:"btn btn-success mt-4" %></div>
        <% end %>