<!--部分テンプレート-->
<%= render 'layouts/users_info', {book:@book , user:@user} %>



<h1>全ユーザの投稿一覧画面</h1>
<siv>左にユーザ情報</siv>
<div>右に投稿一覧</div>
<div>title</div>
<div>body</div>
<% @books.each do |book| %>
<div><%= attachment_image_tag book.user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "NoImage.jpg" %></div>
<div><%= link_to book.title, book_path(book.id) %></div>
<div><%= book.body %></div>
<% end %>

